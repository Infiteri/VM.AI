[project]
name = "vmai-parser-colab"
version = "1.0.0"
description = "VM.AI Parser training for Google Colab"
requires-python = ">=3.10"
dependencies = [
    "transformers>=4.35.0",
    "datasets>=2.14.0",
    "torch>=2.0.0",
    "pyyaml>=6.0",
    "huggingface_hub>=0.19.0",
]

[tool.uv]
dev-dependencies = []
